<UserControl x:Class="HostPilot.Views.ClusterOperationsDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Border Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Cluster Status" />
                    <TextBlock Text="{Binding ClusterStatus}" FontSize="18" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Online Nodes" />
                    <TextBlock Text="{Binding OnlineNodes}" FontSize="18" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Padding="12" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Running Jobs" />
                    <TextBlock Text="{Binding RunningJobs}" FontSize="18" FontWeight="Bold" />
                </StackPanel>
            </Border>
            <Border Padding="12">
                <StackPanel>
                    <TextBlock Text="Alerts" />
                    <TextBlock Text="{Binding Alerts}" FontSize="18" FontWeight="Bold" />
                </StackPanel>
            </Border>
        </StackPanel>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Nodes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Width="220" Height="160" Margin="0,0,12,12" Padding="12">
                        <StackPanel>
                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="16" />
                            <TextBlock Text="{Binding Health}" Margin="0,8,0,12" />
                            <TextBlock Text="{Binding Cpu, StringFormat=CPU: {0:F1}%}" />
                            <TextBlock Text="{Binding Memory, StringFormat=Memory: {0:F1}%}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" />
                <DataGridTextColumn Header="Type" Binding="{Binding EventType}" />
                <DataGridTextColumn Header="Work Item" Binding="{Binding WorkItemId}" />
                <DataGridTextColumn Header="Node" Binding="{Binding NodeId}" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
